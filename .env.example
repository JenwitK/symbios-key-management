# Supabase
NEXT_PUBLIC_SUPABASE_URL=
NEXT_PUBLIC_SUPABASE_ANON_KEY=
SUPABASE_SERVICE_ROLE_KEY=

# App
APP_URL=
VALIDATE_SIGNING_SECRET=

# Discord OAuth (via Supabase)
DISCORD_CLIENT_ID=
DISCORD_CLIENT_SECRET=

# Linkvertise (link monetization)
LINKVERTISE_SECRET=
LINKVERTISE_USER_ID=

# MoonVeil (phase B only)
MOONVEIL_API_KEY=

# Dev only — bypasses Linkvertise verification so the redeem session/HWID
# logic can be tested locally without a real ad click-through. Only takes
# effect when NODE_ENV !== "production"; never set this in a real deploy.
REDEEM_DEV_BYPASS=
